extends layout

block content 

    div(class="section")
        div(class="container")
            form(action="/users/login" method='POST' class="form" id="login")
                h1(class="form_title") Login
                
                div(class="form_input-group")
                    input(type="text" name='username' class="form_input" autofocus placeholder="Username" required)
                    div(class="form_input-error-message")
                
                div(class="form_input-group")
                    input(type="password" name='password' class="form_input" autofocus placeholder="Password" required)
                    div(class="form_input-error-message")
                
                button(class="form_button" type="submit") Continue
                p(class="form_text")
                    a(class="form_link" id="linkCreateAccount" href='/users/register') Don't have an account? Create account.
            if errors
                ul
                    for error in errors
                        li!= error.msg
            